import React from 'react';
import {StyleSheet, View} from 'react-native';

export const CartListSeparator = () => <View style={styles.container} />;

const styles = StyleSheet.create({
  container: {
    height: 5
  },
});
