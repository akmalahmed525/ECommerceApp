import React from 'react';
import {StyleSheet, View} from 'react-native';

export const ProductItemSeparator = () => <View style={styles.container} />;

const styles = StyleSheet.create({
  container: {
    width: 25,
    height: 25,
  },
});
